# Sao chép thành .env rồi chỉnh nếu muốn ghi đè project mặc định:
#   cp web/.env.example web/.env
# (.env đã bị .gitignore — KHÔNG commit .env thật.)
#
# Nếu để trống, web dùng project mặc định trong web/src/lib/config.js
# (project `snjxlsnxrefttupmnkvm`). Đặt 2 biến dưới để trỏ sang project khác.
#
# CHỈ dùng ANON KEY (khóa công khai, bị RLS chặn) — KHÔNG bao giờ đặt service_role ở đây.
VITE_SUPABASE_URL=https://snjxlsnxrefttupmnkvm.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNuanhsc254cmVmdHR1cG1ua3ZtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMxMTQ0NjIsImV4cCI6MjA5ODY5MDQ2Mn0.UX5iN39LEjGaxBZmQweM7peIFoMnXa5nEYG18cQb6IM

# Nguồn dữ liệu build (CI đặt 'live'); web tự suy ra 'live' khi có URL + ANON key.
VITE_DATA_SOURCE=live
