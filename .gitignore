# Sản phẩm build — CI (deploy.yml) tự build từ source, không commit
web/dist/

# Phụ thuộc
node_modules/
.worktrees/

# Biến môi trường cục bộ (chứa key)
.env
.env.*
!.env.example

# File hệ điều hành / editor
.DS_Store
Thumbs.db

# Handoff nâng cấp — CHỈ LOCAL, không push
HANDOFF-NANG-CAP-LOCAL.md

# CHỈ LƯU LOCAL — không công khai trên GitHub (kế hoạch, sao lưu, nguyên tắc, service không dùng)
n8n/
supabase/
docs/
scripts/
services/
AGENTS.md
backups/
